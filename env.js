// Importar las variables de entorno del .env
export const {
    MYSQL_HOST,
    MYSQL_USER,
    MYSQL_PASSWORD,
    MYSQL_DATABASE,
    MYSQL_PORT,
    PORT,
    SECRET,
    UPLOADS_DIR,
    SMTP_SERVICE,
    SMTP_PORT,
    SMTP_USER,
    SMTP_PASS,
    SMTP_API_KEY,
} = process.env;
